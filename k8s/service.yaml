apiVersion: v1
kind: Service
metadata:
  name: cloud-static-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: cloud-static
  ports:
    - protocol: TCP
      port: 80         # Port exposé sur Internet
      targetPort: 5000 # Port à l'intérieur de ton conteneur Flask
